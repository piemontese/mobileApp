import { FieldStepPipe } from './field-step.pipe';

describe('FieldStepPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldStepPipe();
    expect(pipe).toBeTruthy();
  });
});
